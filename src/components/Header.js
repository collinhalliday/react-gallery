import React from 'react';

import Form from './Form';
import Navigation from './Navigation';

const Header = () => (
  <header>
    <Form />
    <Navigation />
  </header>
);

export default Header;
