import React from 'react';

//Renders loading message while app is retreiving Flickr data.
const Loading = () => (
    <div className="loading">
      <p>Loading...</p>
    </div>
);

export default Loading;
